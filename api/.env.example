# Copy to .env and fill in. Never commit the real .env.
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000

# Supabase (Sprint 1+). Service-role key is server-only — keep it secret.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# USDA FoodData Central (ADR-018). Free key from https://fdc.nal.usda.gov/api-key-signup
# Server-only — never exposed to the browser. If unset, the USDA whole-food
# source simply returns no results (the other sources keep working).
USDA_FDC_API_KEY=

# Anthropic — used by the OCR nutrition-label parser (/food-diary/parse-label).
# If unset the endpoint returns 503. Set this in Railway environment variables.
ANTHROPIC_API_KEY=
