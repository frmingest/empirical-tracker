# Copy to .env and fill in. Never commit the real .env.
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000

# Supabase (Sprint 1+). Service-role key is server-only — keep it secret.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
